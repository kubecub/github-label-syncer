#!/bin/bash

for file in "$@"; do
  while IFS= read -r line || [ -n "$line" ]; do
    typo=$(echo "$line" | aspell -a | grep '^&')
    if [ -n "$typo" ]; then
      echo "File: $file, Line: $line, Typo: $typo"
    fi
  done < "$file"
done

if [ -z "$typo" ]; then
  echo "No typos found."
fi
